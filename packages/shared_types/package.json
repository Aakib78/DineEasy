{
  "name": "@dineeasy/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Hand-written TypeScript types mirroring services/api's JSON response shapes — the single place a TypeScript frontend gets these from, instead of re-mirroring them locally (see docs/architecture.md §13).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  }
}
