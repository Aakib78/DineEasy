# Base URL of services/api on the restaurant's LAN. Copy this file to .env.local (Vite loads
# .env.local automatically, and it's gitignored — see docs/local-development.md).
VITE_API_BASE_URL=http://localhost:3000/api/v1
