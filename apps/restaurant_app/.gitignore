.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
build/
*.iml
*.g.dart
*.freezed.dart

# Platform folders are generated by `flutter create .` on a machine with the Flutter SDK
# (see docs/flutter-app.md) and are environment-specific, so they're not hand-authored or
# committed here.
/android/
/ios/
/windows/
/linux/
/macos/
/web/
