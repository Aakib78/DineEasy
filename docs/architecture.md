# DineEasy Architecture

## 1. System overview

DineEasy is a single deployable backend (NestJS API + PostgreSQL + Redis) that serves three clients:

- **Restaurant app (Flutter)** — staff-facing: POS, tables, KDS, menu/staff admin. Android + Windows.
- **Customer QR web (Vite/React PWA)** — guest-facing: scan table QR → menu → order → track. No account required.
- **Admin/reporting** — v1 ships owner/manager reporting inside the Flutter app rather than a separate web admin, to avoid building and maintaining a fourth UI before the operational core is solid. A browser-based admin is a natural P2 addition once the API is stable, and nothing in the API is Flutter-specific.

All three talk to the same versioned REST API (`/api/v1/...`) plus a WebSocket gateway for real-time events. There is one source of truth: PostgreSQL. Redis is used for short-lived state (rate limiting, WebSocket pub/sub fan-out across API replicas, idempotency keys) — it is not the system of record, so a Redis restart never loses an order.

```
                         RESTAURANT LAN
                            Router
                              │
              ┌───────────────┼───────────────┐
              │               │               │
        Flutter POS      Flutter KDS     Customer phone
        (staff Wi-Fi)     (staff Wi-Fi)    (guest Wi-Fi)
              │               │               │
              └───────────────┬───────────────┘
                              ▼
                    DINEEASY SERVER (Docker)
                    ┌─────────────────────┐
                    │   NestJS API (v1)    │
                    │  REST + WebSocket    │
                    └──────────┬──────────┘
                     ┌──────────┴──────────┐
                     ▼                     ▼
               PostgreSQL              Redis
             (system of record)   (cache/pubsub/rate-limit)
```

Guest Wi-Fi and staff Wi-Fi are modeled as **two SSIDs on the same router, both able to reach the DineEasy server's LAN IP/port**. This is the restaurant's network configuration, not something DineEasy manages — `docs/deployment.md` documents the requirement (guest VLAN, if used, must still route to the server) and the health screen (§12) surfaces it if the customer web can't be reached from the guest network.

## 2. Why this stack

| Concern | Choice | Why |
|---|---|---|
| Backend | NestJS + TypeScript | Modular DI architecture maps directly onto the domain module list in the spec; mature ecosystem for guards/interceptors/validation needed for RBAC, tenant isolation and idempotency. |
| ORM / migrations | Prisma | Schema-first migrations, generated types shared with DTOs, good transaction support for financial writes. Chosen over TypeORM for migration reliability and simpler raw-SQL escape hatches for report queries. |
| Database | PostgreSQL | Relational integrity for financial/tenant data, `NUMERIC` for money, strong constraint support. Runs happily on a single restaurant-grade PC. |
| Cache/realtime backbone | Redis | Idempotency key storage, WebSocket adapter (so a future multi-replica deployment fans out events correctly), rate limiting. Never the source of truth. |
| Real-time | Socket.IO (NestJS WebSocket gateway) | Auto-reconnect and room support out of the box; clients treat every event as a hint to refetch, never as the only copy of truth (§8). |
| Staff client | Flutter (Riverpod) | One codebase for Android tablets + Windows POS, per spec. Riverpod for predictable, testable state without global mutable singletons. |
| Local staff storage | Drift (SQLite) | Mature, well-tested relational local DB with reactive streams — a good fit for cached menu, active orders, and a durable sync/outbox queue (§7). |
| Customer client | Vite + React + TypeScript PWA | No app-store install, instant load on a guest phone, small bundle, installable as PWA for repeat customers. |
| Containerization | Docker Compose | Meets the "no Kubernetes, must run on a restaurant LAN PC" requirement directly. |

## 3. Multi-tenancy model

```
Organization  (the paying customer — a restaurant business)
    └── Outlet   (a physical location; v1 ships single-outlet-per-org UX but the schema supports many)
          ├── Users (staff, scoped to the org, roles scoped per outlet)
          ├── Floors → Tables → QR codes
          ├── Menu → Categories → Items → Variants / Modifier groups
          └── Orders, Dining sessions, Invoices, Payments, Audit logs
```

Every tenant-scoped table carries `organization_id` (and `outlet_id` where relevant) as a required foreign key. **The organization/outlet ID is never taken from the request body or query string for authorization purposes** — it is resolved from the authenticated JWT's claims (or, for QR customer requests, from the dining-session token) and injected by a `TenantContext` request-scoped provider. Every repository query goes through this context. This is enforced with a Prisma middleware (`prisma-tenant-guard`) that requires `organizationId` on every query against a tenant-scoped model and throws if a handler forgot to scope it — a missing tenant filter fails closed, not open.

## 4. Domain model (see `docs/database.md` for full schema)

Core aggregates:

- **Organization / Outlet / User / Role / Permission** — identity & tenancy.
- **Floor / Table / TableQrCode** — physical layout + QR identity. QR encodes an opaque signed token, never raw IDs (§ QR below).
- **DiningSession** — represents an occupancy of a table by one or more guests from QR scan to table close. Multiple guests scanning the same table QR join the same open session (§ Dining sessions below).
- **Menu / MenuCategory / MenuItem / MenuItemVariant / ModifierGroup / Modifier** — flexible catalog; availability flags read by both POS and QR.
- **Order / OrderItem / OrderItemModifier / OrderEvent** — the single order model for POS, waiter and QR origins (§ Orders below). `OrderEvent` is the audit trail of every state transition.
- **KitchenStation / KitchenOrder / KitchenOrderItem** — KOT derived from an Order, routed to one or more stations.
- **Invoice / InvoiceItem / Tax** — billing snapshot generated from an Order at bill time; immutable once issued.
- **Payment / PaymentTransaction / Refund** — provider-independent payment record (§ Payments below).
- **Printer / PrinterJob** — abstracted print queue (§ Printing below).
- **AuditLog** — generic append-only log for sensitive mutations, separate from `OrderEvent` (which is order-domain-specific and drives KOT/KDS).

## 5. Order domain — single model, state machine

POS, waiter and QR orders all create the same `Order` aggregate; the only difference is `Order.source` (`POS | WAITER | QR`) and, for QR, a `diningSessionId`. There is exactly one order state machine, implemented as a pure domain function (`services/api/src/modules/orders/domain/order-state-machine.ts`) with no framework dependencies, so it is unit-testable in isolation and cannot be bypassed from a controller:

```
DRAFT → PLACED → ACCEPTED → PREPARING → READY → SERVED → BILLED → PAID → COMPLETED
                                                              └──────────→ CANCELLED
                                                                              └──→ REFUNDED
```

Rules (enforced in the domain layer, not in UI):
- Only whitelisted transitions are allowed; any other transition throws `InvalidOrderTransitionError` (mapped to HTTP 409).
- `CANCELLED` is reachable from any pre-`PAID` state; `REFUNDED` only from `PAID`/`COMPLETED`.
- Every transition writes an `OrderEvent` row (actor, from-state, to-state, timestamp, reason) — this is what auditability and KDS "elapsed time" are built on.
- Financial fields (`subtotal`, `taxTotal`, `discountTotal`, `total`) are **recomputed server-side on every mutation** from current menu prices/tax config; the client never supplies a trusted total (§10, §14 of the spec — enforced in `OrdersService.recalculateTotals`).

## 6. Dining sessions

```
Table 12
   │
   └── DiningSession (status: OPEN)
          ├── Guest A (guestToken A) → Order #1
          ├── Guest B (guestToken B) → Order #2 (same session, same table)
          └── Guest C (guestToken C) → Order #3
```

Scanning a table QR resolves the token to `{organizationId, outletId, tableId}`, then finds-or-creates the table's currently `OPEN` `DiningSession`. Each browser tab gets a lightweight, unguessable `guestToken` (random, stored in memory + `localStorage` on the customer PWA, not tied to any personal data) so multiple phones ordering into the same session can be told apart in the POS UI without requiring login. A session closes when staff explicitly close the table (after billing) — this is a staff action, not a client-side timeout, so a dropped connection never silently loses a guest's session.

## 7. Offline / LAN-first architecture

**Server side:** the API, Postgres and Redis all run inside the restaurant LAN via Docker Compose (`infrastructure/docker/docker-compose.yml`). No core operation (auth, menu, tables, orders, KOT, KDS, billing, local QR ordering, local reports) calls out to the internet. Only `PaymentsModule` online-provider calls and future cloud-sync calls touch the internet, and both are designed to fail into a clearly-surfaced "unavailable, use offline workflow" state rather than blocking the order flow (`docs/offline-mode.md` has the full failure-mode table).

**Flutter client side:** the restaurant app is not just a thin REST client — it keeps a local Drift database mirroring what the current device needs: cached menu/config, active orders, and a **mutation outbox**. Every write (create order, update KOT status, take payment) is first written to the local outbox with a client-generated idempotency key, applied optimistically to local state, then drained to the server by a sync worker with exponential backoff. Server responses are matched back to outbox entries by idempotency key so a retried request never double-creates an order (§11 of the spec, detailed in `docs/offline-mode.md`). If the Wi-Fi drops mid-shift, staff keep working against local state; when the LAN connection to the server is lost too (not just internet), the app surfaces a clear "can't reach DineEasy server" banner rather than silently queueing forever, because unlike an internet outage, a LAN-server outage means the kitchen and other terminals are also not seeing new orders.

**Customer PWA:** requires the LAN (it talks straight to the server for menu/order placement — there is no offline order queue on the guest side, since an order that isn't visible to the kitchen isn't useful); it degrades by showing a clear "can't reach the restaurant" state with a retry rather than a blank page.

## 8. Real-time

WebSocket gateway (`RealtimeGateway`, namespace `/realtime` — `services/api/src/common/realtime/realtime.gateway.ts`) actually emits four events: `order.updated`, `table.updated`, `kitchen.queue_updated`, `notification.created` (an earlier version of this doc listed a larger, more granular set — `order.created`/`order.cancelled`/`kot.created`/`kot.updated`/`kitchen.started`/`kitchen.ready`/`payment.updated` — that was never actually implemented; the smaller set above is what ships, and every one of those finer-grained cases collapses into `order.updated`, which is enough since the contract below means a client always just goes and re-fetches). Clients join rooms scoped to `outlet:<outletId>` (staff — every role sees every event for their outlet, kitchen included, there's no separate per-station room) or `session:<diningSessionId>` (a guest, their own dining session only) so events never cross tenants or leak one guest's order to another. Every event payload carries only an id, never a full trusted snapshot — **on receiving an event, the client re-fetches the affected resource from the REST API**, so a missed or out-of-order WebSocket message can never leave a client showing stale-but-confident data. The database remains the sole source of truth; WebSockets are a "something changed, go look" signal, not a data channel. Consumed by the customer PWA (`apps/customer_web/src/lib/realtime/useOrderUpdates.ts`, since that app was built), the Flutter staff app (`apps/restaurant_app/lib/core/realtime/`, layered on top of — not replacing — its existing polling; see `docs/flutter-app.md`), and now the POS web app too (`apps/pos_web/src/lib/realtime/RealtimeContext.tsx`, see `docs/pos-web.md`) — three independent clients of the same gateway, none of them trusting an event as anything more than a hint to go re-fetch.

## 9. Payments

Abstracted behind `PaymentProvider` (`services/api/src/modules/payments/domain/payment-provider.interface.ts`) with `PaymentIntent`, `PaymentTransaction`, `Refund` as domain types independent of any vendor SDK. v1 ships a `ManualPaymentProvider` (cash / UPI-shown-as-QR / card, all staff-confirmed at the POS — accurate for how most Delhi/Faridabad QSRs actually take payment today) implementing the same interface a future Razorpay/PhonePe-style provider would. Webhook endpoints (`/api/v1/payments/webhooks/:provider`) verify a provider signature, are idempotent on `providerEventId`, and only update `PaymentTransaction` status — they never trust a client-reported "payment succeeded" call (§14/§45 of the spec).

## 10. Printing

`PrintersModule` models a `Printer` (station-scoped, kitchen or receipt) and a `PrinterJob` queue (`payload`, `status: QUEUED|SENT|FAILED|ACKED`). The domain layer only ever enqueues jobs; a separate, swappable process — `services/print-agent`, a standalone Node daemon meant to run on the restaurant's LAN, *not* bundled into the API itself — polls the queue and sends a raw ESC/POS-over-network ticket to the printer. This keeps `KitchenModule`/`BillingModule` free of any printer-vendor code — see `docs/printing.md`.

## 11. Security posture

- Argon2/bcrypt password hashing, short-lived access JWT + rotating refresh token (stored hashed, revocable per-device) — `docs/authentication.md`.
- RBAC via a `permissions` table and a `@RequirePermission('orders.cancel')` decorator/guard — never a hard-coded role string check in a handler.
- `TenantContext` guard (§3) on every tenant-scoped route.
- `class-validator` DTOs on every input; global `ValidationPipe({ whitelist: true, forbidNonWhitelisted: true })`.
- Rate limiting (`@nestjs/throttler` + Redis store) on auth and payment webhook routes.
- `helmet` security headers, explicit CORS allowlist from `CORS_ORIGINS`.
- All monetary math via `decimal.js` / Postgres `NUMERIC`, never native floating point.
- Structured logging (`pino`) with a request-correlation ID on every log line; secrets/tokens never logged.

## 12. Observability & supportability

Every request gets an `x-request-id` (generated if absent) threaded through logs. A `/api/v1/system/health` endpoint reports API/DB/Redis reachability; the Flutter app's Settings → System Status screen renders this plus its own local view of printer/LAN/internet connectivity — the spec's §59 health screen. `/api/v1/system/version` exposes app/API/migration versions for the versioning/compatibility requirement (§61).

## 13. Repository layout mapping

See root `README.md` for the directory tree. `packages/shared_types` (an npm workspace, `@dineeasy/shared-types`) holds hand-written TypeScript types mirroring `services/api`'s JSON response shapes, consumed by both TypeScript web apps — `apps/customer_web` (whose own `src/lib/api/types.ts` is just a re-export of it — see `docs/customer-web.md`) and, since `apps/pos_web` was built, `apps/pos_web` too (see `docs/pos-web.md`), which is what pushed this package past "the customer web app's types" into a genuinely shared one: it now also exports the RBAC `PERMISSIONS` catalog, `generateId()` (the `crypto.randomUUID()` secure-context workaround), and the centralized color `theme`/`applyTheme()` both web apps' `main.tsx` calls before their first render. "Hand-written" rather than generated from the OpenAPI spec `@nestjs/swagger` now produces (`docs/api.md`) because generating from it means bootstrapping the live API, which needs a working `@prisma/client` this sandbox can't produce (`docs/troubleshooting.md`) — switching to spec-generated types is the natural next step once that's no longer true, and nothing about how these are written fights that migration. The Flutter app has its own Dart models (`lib/features/*/data/*_models.dart`, hand-mirrored per screen) and its own `colorSchemeSeed`/`Permissions` class kept in sync by hand with the two catalogs above, since Dart can't consume a TypeScript package regardless of how it's produced — see `docs/flutter-app.md`. `services/print-agent` (also an npm workspace, `@dineeasy/print-agent`) is a fifth, independent thing again: a standalone Node process meant to run on the restaurant's LAN rather than ship as part of `services/api` — see `docs/printing.md` and its own README.md.

## 14. Explicit non-goals for v1

Per spec §64: no delivery marketplace, no restaurant discovery, no social features, no payroll/accounting suite, no supplier marketplace, no advanced CRM/loyalty, no reservations marketplace, no AI recommendations, no multi-country tax engine, no inventory ERP, no microservices, no Kubernetes.

## 15. Build status

This section is updated as slices land — it is the honest source of truth for "what actually works today" vs. what is designed but not yet built.

**Done:**
- Monorepo structure, `.env.example`, root docs (this file + `docs/*.md`).
- `infrastructure/docker/docker-compose.{yml,dev.yml,test.yml}`.
- Full Prisma schema (`services/api/prisma/schema.prisma`) — 39 tables, 18 enums.
- Initial migration, hand-authored (see `docs/troubleshooting.md` for why) and **validated by applying it to a real PostgreSQL 16 database** — 39 tables / 18 enum types / 56 foreign keys created with zero errors.
- `services/api` project scaffold: `package.json`, TypeScript config, ESLint/Prettier, `Dockerfile`.

- NestJS application bootstrap (`main.ts`, `AppModule`) wired with: global validation, helmet, CORS, `nestjs-pino` structured logging with request-id correlation, a global `DomainExceptionFilter`, rate limiting (`@nestjs/throttler`).
- Tenant isolation: `TenantContextStore` (AsyncLocalStorage) + `TenantContextInterceptor` + a Prisma `$use` middleware in `PrismaService` that fails closed on any tenant-scoped query missing an `organizationId`/`outletId` filter, with a narrow, grep-able `runUnscoped()` escape hatch for the handful of legitimately pre-tenant operations (login-by-email, refresh-token lookup, health checks). See `docs/architecture.md` §3.
- RBAC: `PERMISSIONS`/`SYSTEM_ROLES` catalog (`common/rbac/permissions.catalog.ts`), `RolesService` (seeds the 5 system roles + full permission catalog per organization, computes a user's effective permissions), `@RequirePermission(...)` decorator + `PermissionsGuard`, `@Public()` decorator + `JwtAuthGuard`.
- `AuthModule`: `POST /api/v1/auth/register` (creates Organization + Owner atomically — spec §68 step 1), `login`, `refresh` (rotating refresh tokens, hashed at rest), `logout`. Argon2id password hashing.
- `OrganizationsModule`, `OutletsModule` (create/list/get/update — spec §68 "Creates restaurant" → "Creates outlet"), `UsersModule` (staff CRUD + role assignment), `RolesModule` (list roles/permissions), `AuditModule` (generic audit trail wired into org/outlet/staff mutations), `SystemModule` (`/system/health`, `/system/version` — spec §59/§61).
- `prisma/seed.ts`: demo organization, 2 floors/10 tables with QR codes, GST 5% tax group, 3 menu categories / 23 items with variants and modifier groups, 5 staff accounts (one per role).
- `TaxModule` (`TaxGroup`/`TaxGroupComponent` CRUD — CGST/SGST/IGST/SERVICE_CHARGE components per spec §16), `ModifiersModule` (outlet-scoped, reusable `ModifierGroup`s, spec §12), `MenuModule` (categories, items, variants, modifier-group attachment, `getFullTree` for staff editing vs. `getPublicTree` for the QR customer view, price-change-aware audit logging).
- `TablesModule`: floors + tables CRUD, `TableQrCode` issued on table creation, in-place QR token rotation (`POST /tables/:id/qr/regenerate`) — a table has exactly one QR row (`tableId @unique`), so regenerating rotates the token rather than creating a second row, and any dining session already open on that table is unaffected.
- `DiningSessionsModule`: `DiningSessionsService` (`findOrCreateOpenSession`, `getById`, `listOpenForOutlet`, `close` — refuses to close while any order on the table is still active) + staff-facing `DiningSessionsController` (`GET /dining-sessions`, `GET /dining-sessions/:id`, `POST /dining-sessions/:id/close`, all permission-gated on `tables.view`/`tables.manage`). Sessions are never created by staff — only by a QR scan.
- Guest/QR authentication foundation: `GuestAuthModule` (`@Global()`), `GuestTokenService` (signs/verifies dining-session bearer tokens with their own `QR_TOKEN_SECRET`, deliberately isolated from the staff JWT secret, 12h TTL), `DiningSessionGuard` (verifies the guest bearer token and attaches `request.diningSession`), `@CurrentDiningSession()` param decorator. `TenantContextInterceptor` binds `isGuest: true` sessions into `TenantContextStore` exactly like a staff user, so downstream Prisma calls are scoped identically either way.
- `QrModule`: the full no-signup customer entry point (spec §4/§6/§12) — `POST /qr/:token/resolve` (`@Public()`) resolves the opaque table token (the one legitimate `TenantContextStore.runUnscoped()` use in this slice — the token *is* the tenant lookup), joins-or-creates the table's open `DiningSession`, mints a per-tab `guestToken` so multiple diners at one table each get their own session token, and returns a signed dining-session bearer token; `GET /qr/menu` (`@Public()` + `DiningSessionGuard`) serves `MenuService.getPublicTree()` for that session's outlet.
- `Dockerfile` (multi-stage dev/build/production) + `.dockerignore`.
- **The unified order domain** (spec §7/§31): `OrdersModule` — `OrdersController` (POS/Waiter) and `OrdersGuestController` (QR, dining-session-token-gated) both create/mutate the *same* `Order` model through one `OrdersService`. Covers: create (menu-validated, decimal-safe pricing via `order-pricing.util.ts`, idempotency-key-safe for offline retry), add items to a live order, cancel an item, apply one order-level discount, accept/serve/cancel, and the full state machine (`common/order/order-state-machine.ts`) gating every transition. A dine-in order auto-opens (or reuses) the table's `DiningSession`.
- **KOT/KDS**: `KitchenModule` — `OrdersService` creates a `KitchenOrder`+`KitchenOrderItem`s in the same transaction as order placement/item-add (v1 routes every KOT to a single unrouted queue — no per-menu-item station mapping exists in the schema yet, a documented gap, not a bug). `KitchenService` exposes the live queue and per-item status updates (NEW→PREPARING→READY→COMPLETED/CANCELLED), and *derives* the order's PREPARING/READY status from item progress — never a client-set flag.
- **Billing**: `BillingModule` — turns a SERVED order into an immutable `Invoice` with a recomputed CGST/SGST/IGST breakdown (`InvoiceTax`), idempotent (re-calling returns the existing invoice rather than duplicating).
- **Payments**: `PaymentsModule` — cash/UPI/card recorded by staff at the counter (`recordPayment`, supports split payment across multiple calls), auto-transitions BILLED→PAID→COMPLETED once the total is covered; a provider-webhook endpoint + `PaymentTransaction` idempotency (unique `(provider, providerEventId)`) is wired end-to-end as a scaffold for a future real gateway, even though no v1 provider calls it yet (see `ProviderWebhookDto`); basic refund initiate/approve flow.
- `common/counters/daily-counter.service.ts`: atomic, per-outlet-per-day sequential numbering (order/KOT/invoice numbers) via a Postgres `upsert` (`INSERT ... ON CONFLICT DO UPDATE`), always called inside the caller's transaction so a rolled-back order never burns a number.
- Unit tests: `order-pricing.util.spec.ts` (12 tests — modifier pricing, GST component splitting, decimal-precision, service-charge-on-post-discount-base, round-off) — **passing** in this environment, alongside the earlier 13.
- **Real-time "refetch hint" channel** (spec §8/§34): `RealtimeModule`/`RealtimeGateway` — a single in-process Socket.IO instance (no Redis adapter in v1, matching the LAN-first single-server deployment model), staff JWT or guest dining-session token verified independently at handshake, room design `outlet:<outletId>` (all staff surfaces) vs. `session:<diningSessionId>` (a guest's own session only — never the outlet-wide room). Every event is a contentless `{type, ids}` nudge, never authoritative data — `OrdersService`, `KitchenService`, and `DiningSessionsService` emit `order.updated`/`kitchen.queue_updated`/`table.updated` after their respective state changes commit.
- **Reports**: `ReportsModule` — `GET /reports/sales-summary`, `GET /reports/top-items` (Prisma `groupBy` on `OrderItem`), `GET /reports/payment-breakdown` (`groupBy` on `Payment.method`), all outlet-scoped, date-range-filterable, gated on `reports.view`.
- **Printers**: `PrintersModule` — `Printer` CRUD (network/USB, station- or outlet-scoped) and the `PrinterJob` queue (`QUEUED→SENT/FAILED→ACKED`, auto-retry up to 3 attempts, `GET /printers/:id/jobs/next` + `PATCH /printers/jobs/:jobId/status` as the contract a print agent polls). `OrdersService` enqueues a `KITCHEN` job on every KOT creation and `BillingService` enqueues a `RECEIPT` job on every invoice, both best-effort/fail-open so a missing or offline printer never blocks taking an order or billing a table. The print agent that actually drains this queue is `services/print-agent` — see below.
- Two pre-existing tenant-guard bugs found by code review and fixed while wiring the above: `OutletsService.update()` and `DiningSessionsService.close()` were both calling Prisma's bare `.update({where:{id}, ...})` on models in the tenant guard's org-scoped list (`Outlet`, `DiningSession`) — the guard unconditionally refuses `findUnique`/`update`/`delete` on any tenant-scoped model (Prisma's `update()` can't accept an injected `organizationId` filter in `where`), so both would have thrown at runtime. Both now use `updateMany({where:{id, organizationId}, ...})` plus a re-fetch. A full-codebase grep for every other bare `.update(`/`.findUnique(`/`.delete(` call confirmed no other call site is affected.
- A third, narrower cross-tenant gap found and fixed the same way: `PrinterJob` carries no `organizationId`/`outletId` of its own, so it sits outside the Prisma tenant-guard middleware's model list entirely — `PrintersService.updateJobStatus` was scoping only by `jobId`, meaning any staff member holding `printers.manage` at *any* outlet could patch a print job belonging to a different organization's outlet if they had (or guessed) its UUID. Fixed by joining through `printer: { outletId }` in the lookup and threading the caller's active outlet through the controller. Low real-world exposure (UUIDs aren't guessable) but a real gap in "isolation enforced server-side, not just by convention" — worth closing rather than leaving to luck.
- Unit tests: `order-state-machine.spec.ts` (14 tests — the full legal-transition table, the one documented `READY -> PREPARING` backward edge, illegal jumps, terminal-state refusals for both `OrderStatus` and `KitchenItemStatus`) and `reports.service.spec.ts` (pure `resolveRange`/`startOfDay` date-window logic — written and correct, but like `auth.service.spec.ts`, not executable in this sandbox because the file under test imports `PrismaService`, which ts-jest type-checks eagerly against the un-generated Prisma client; it will run the moment `prisma generate` runs on a normal dev machine).

**Verified in this environment**, despite the Prisma engine restriction: `npx tsc --noEmit` passes with zero errors outside of Prisma's un-generated types (confirmed line-by-line — every remaining error is an implicit-`any` on a `Prisma.TransactionClient`/`Prisma.Middleware`-typed parameter, or on a value whose type chains back to one, which resolves once `prisma generate` runs — **49**, down from 54 across the payments and kitchen test-coverage passes below, both of which fixed pre-existing implicit-`any` parameters as a side effect of extracting the logic at those exact call sites into typed pure functions, not a deliberate cleanup pass); `npx eslint` is clean (zero errors, 5 pre-existing warnings in `auth.service.spec.ts`); the initial migration and the notifications migration were both applied to a live Postgres 16 and matched their schemas exactly (§ above); **86** pure-logic unit tests (permissions, duration, order pricing, order/kitchen state machine, notification visibility, invoice tax aggregation, active-outlet guard, payment/refund math, kitchen status derivation, order settled-status, guest token sign/verify) pass with zero Prisma dependency.

- **Flutter staff app foundation + auth** (`apps/restaurant_app`, spec §2/§60 — Android tablets primary, Windows back-office secondary): `ApiClient` (Dio, auth-header injection, single-flight refresh-and-retry on 401), `AuthRepository`/`AuthSessionNotifier` (Riverpod) against `AuthController`, OS-keychain token storage (`flutter_secure_storage` — tokens are never in plain SharedPreferences), a hand-decoded-JWT claims model (no separate "who am I" endpoint exists server-side), a client-side mirror of the permission catalog for UI gating only (never a security boundary — `PermissionsGuard` on the server is), `go_router` with an auth-state-driven redirect, and a permission-filtered navigation shell around placeholder screens for every feature area. See `docs/flutter-app.md` for the full breakdown, the architecture rationale, and — importantly — its verification status.

- **Flutter staff app — POS screen** (`apps/restaurant_app/lib/features/pos/`): the first real feature screen behind the nav shell, replacing the POS destination's placeholder. `PosHomeScreen` (color-coded floor/table grid + a Takeaway entry point) and `OrderBuilderScreen` (category-tabbed menu, variant/modifier selection via `ItemCustomizeSheet`, a `CartPanel` bottom bar, submit to either `POST /orders` or `POST /orders/:id/items` depending on whether the tapped table already has an order open). A repository layer (`MenuRepository`/`TablesRepository`/`OrdersRepository`) wraps the same endpoints the backend's `OrdersService` prices authoritatively — every total shown while building a cart is a client-side estimate only, exactly the same philosophy as `apps/customer_web`'s pricing. Two real bugs were caught during hand-review (money formatted as a string being compared lexicographically instead of numerically when finding a multi-variant item's cheapest price; a cart-line id weak enough for a fast double-tap to collide on) and fixed — see `docs/flutter-app.md` for detail. A new pure-logic test file, `test/features/pos/pos_cart_test.dart` (9 tests over `PosCartNotifier`'s state transitions and subtotal math), joins the existing zero-Flutter-dependency suites; none of these can actually run in this sandbox for the reasons below.

- **Flutter staff app — Tables management** (`lib/features/tables/tables_management_screen.dart`): floor/table *setup*, separate from the POS's read-only table picker above — add a floor, add a table (name, seats, floor), edit a table (rename, reseat, change `status`, rotate its QR token via `POST /tables/:id/qr/regenerate`, which leaves any dining session already open on that table unaffected). Reuses the POS feature's `Floor`/`RestaurantTable`/`TablesRepository` rather than duplicating them (`pos_models.dart` gained `RestaurantTable.qrCode`/`.displayOrder` for this screen's benefit). Create/edit controls are further gated on `tables.manage` client-side (the tab itself only requires `tables.view`) so a view-only role isn't shown a button guaranteed to 403 — UX only, `PermissionsGuard` on the server is the real gate. See `docs/flutter-app.md` for the one specific unverified-API-surface risk this slice introduces (`DropdownButtonFormField`'s `value:` vs. `initialValue:` naming across Flutter SDK versions).

- **Customer QR ordering PWA** (`apps/customer_web` — Vite + React + TS, spec §4/§6/§12): session bootstrap against `POST /qr/:token/resolve` (`sessionStorage`-scoped dining-session token, matching the backend's per-tab `guestToken` model), menu browsing with variant/modifier selection, a client-side cart (`sessionStorage`, nothing created server-side until "Place order," per docs/qr-ordering.md), decimal-safe (never-float) price *estimates* clearly labeled as such — the backend remains the sole authority on the real total — order placement via `POST /qr/orders`, and an order-tracking screen with a status stepper that stays live via a `RealtimeGateway` WebSocket subscription (`session:<diningSessionId>` room) with a 15s poll fallback. See `docs/customer-web.md` for the full breakdown. Unlike the Flutter app below, **this one was actually built and verified in this sandbox** — `npm run build`/`lint` both pass clean, and the production bundle was smoke-tested by serving it — because Node/npm have working network access here, unlike the Flutter SDK or Prisma's engine binaries; the Flutter app has since been verified for real too, just not in this sandbox — see the "First full local run end-to-end" bullet below.

- **Docker/LAN validation pass**: exercised the compose files for the first time since the foundation slice wrote them (previously only read, never run) and found three real bugs purely from actually running them — an invalid stray character that broke YAML parsing outright, a `.env`-auto-load gotcha specific to running `docker compose` with `-f` files outside the current directory, and a required-variable interpolation-order issue in the test overlay — all fixed, see `docs/troubleshooting.md` for the full detail on each. Also added `apps/customer_web/Dockerfile` (referenced by the base compose file since the foundation slice but never created, since the customer PWA didn't exist until this session) and, in fixing it, caught the `web` service passing a wrong-named, wrong-mechanism env var (`VITE_API_URL` as a runtime `environment:` entry — the app reads `VITE_API_BASE_URL`, and Vite bakes it in at *build* time for a static production bundle) — fixed as a `build.args` entry for production and a real runtime env var only for the dev-server override, where that actually works. All three compose combinations (base, `+dev`, `+test`) now pass `docker compose config --quiet`. Separately, re-verified the initial migration from scratch against a fresh Postgres 16 instance — 39 tables / 18 enum types / 56 foreign keys, an exact match to what's documented above — using a natively `apt`-installed Postgres and Redis rather than the Docker images the compose files call for, because this sandbox's network allowlist covers the npm registry and the Ubuntu package archives but not Docker Hub (`registry-1.docker.io` 403s, same restriction family as `binaries.prisma.sh`). **What this does and doesn't prove**: the compose files are now syntactically and semantically correct and the schema is independently re-confirmed correct — but an actual `docker compose up` (pulling `postgres:16-alpine`/`redis:7-alpine`/`node:20-alpine`/`nginx:1.27-alpine` and running real containers) has still never happened in any environment this project has been built in so far, and neither has a live, running API server anywhere (still blocked on `prisma generate`, per the Prisma section above). Both remain open items for a machine with normal internet access.

- **Flutter staff app — Kitchen display (KDS)** (`lib/features/kitchen/kds_screen.dart`): a horizontally-scrolling board of KOT tickets, oldest first, each item advanceable NEW → PREPARING → READY → COMPLETED (or CANCELLED at any point before COMPLETED) via `PATCH /kitchen/items/:id/status`; a ticket drops off the board once every item on it is done, mirroring `KitchenService.listQueue`'s own `notIn: ['COMPLETED', 'CANCELLED']` filter. Order status is never written from here — the backend derives it from item-level progress. Kept live with a 6-second `Timer.periodic` re-fetch *and* a `RealtimeGateway` `kitchen.queue_updated`/`order.updated` subscription layered on top (the near-term follow-up flagged here previously — see `docs/flutter-app.md`'s realtime changelog entry) — the poll stays on deliberately as a backstop, not removed now that the socket exists. Update controls are gated on `kitchen.update` client-side (the tab itself only needs `kitchen.view`). New pure-logic tests: `test/features/kitchen/kds_models_test.dart` (9 tests). See `docs/flutter-app.md` for detail.

- **Flutter staff app — Billing** (`lib/features/billing/`): `BillingScreen` lists orders ready to bill (SERVED) or awaiting payment (BILLED/PAID), filtering `activeOrdersProvider` client-side rather than a dedicated endpoint. `BillingDetailScreen` covers "Generate bill" (`POST /orders/:id/invoice`, idempotent server-side), a GST tax-line breakdown from the resulting invoice, and a payment form (`POST /orders/:id/payments`) supporting split/partial payments — defaulting to and re-syncing against the *remaining* balance, not the order total, since the backend only advances the order past BILLED once the running SUCCEEDED total covers it in full. Never writes order/payment status directly; reflects whatever the backend derives (`PaymentsService.settleOrder` for the PAID → COMPLETED auto-advance). Extended the shared `Order` model with `subtotal`/`discountTotal`/`taxTotal`/`serviceChargeTotal`/`tableName`/`payments` (all already present on every order response, just newly parsed) and added `Money.operator-`/`Money.toPlainString()`. Caught and fixed a real bug during hand-review: an anchored-regex `FilteringTextInputFormatter.allow` for capping the payment amount at 2 decimals would have wiped the whole field on one keystroke past the limit (an `allow` filter keeps only matching *substrings*, and an anchored `^...$` pattern can only match the entire string or nothing) — fixed with `TextInputFormatter.withFunction`. New pure-logic tests: `test/features/billing/billing_models_test.dart` (3 tests) plus 4 new `money_test.dart` cases. See `docs/flutter-app.md` for full detail.

- **Flutter staff app — Reports** (`lib/features/reports/`): `ReportsScreen`, a read-only dashboard over the three `reports.view`-gated endpoints (`GET /reports/sales-summary`, `/reports/top-items`, `/reports/payment-breakdown`) — a date-range preset picker (Today/Last 7 days/Last 30 days, `ReportRangePreset`) plus three independently-loading sections: the sales summary (revenue, order/dine-in/takeaway counts, average order value, a subtotal→discount→tax→service-charge→revenue breakdown), the server-ordered/server-limited top-items list, and a per-payment-method breakdown. No mutating controls exist on this screen, so unlike every other feature slice there's no per-control RBAC gating beyond the tab-level `reports.view` check the nav shell already does. Caught and fixed a real bug during hand-review: the resolved date range was originally a private provider that only recomputed when the selected preset changed, not on a timer — since `to` is captured via `DateTime.now()` inside it, pulling to refresh would have silently kept re-fetching the exact same frozen timestamp instead of advancing to the current time. Fixed by making the provider non-private (`resolvedReportRangeProvider`) and having the screen's pull-to-refresh explicitly invalidate it alongside the three report providers. New pure-logic tests: `test/features/reports/reports_models_test.dart` (6 tests). See `docs/flutter-app.md` for full detail.

- **Flutter staff app — Staff management** (`lib/features/staff/`): `StaffScreen` lists every staff account (`GET /staff`) with a status badge and a chip per role assignment. Add/edit bottom sheets (gated on `staff.manage`, tab itself only needs `staff.view`) create accounts (`POST /staff`) and update name/phone/status/role assignment (`PATCH /staff/:id`). A staff member can hold multiple simultaneous role assignments across outlets — the backend only deletes-and-recreates the `UserRole` row matching the exact `(userId, outletId)` pair it's given, so reassigning someone to a different outlet adds a role rather than replacing their existing one; the UI surfaces every assignment as its own chip rather than implying a single current role. New repositories: `StaffRepository`, `RolesRepository` (`GET /roles`, just enough for the role dropdown), `OutletsRepository` (`GET /outlets`, ungated beyond sign-in). This is this app's first use of a nullable `DropdownButtonFormField<String?>` (a `null`-valued "No change"/"Org-wide" sentinel item) — every earlier dropdown in the codebase used a non-nullable value. New pure-logic tests: `test/features/staff/staff_models_test.dart` (9 tests). See `docs/flutter-app.md` for full detail.

- **OpenAPI docs + `packages/shared_types`**: `@nestjs/swagger` wired into `main.ts` (`SwaggerModule`, served at `/api/docs` + `/api/docs-json`, gated on `API_DOCS_ENABLED` — default on outside `production`) with its CLI plugin enabled in `nest-cli.json` so every DTO's schema is inferred automatically from its TypeScript types + `class-validator` decorators, with no per-field `@ApiProperty()` annotation needed across any of the ~15 modules. `packages/shared_types` now exists as a real npm workspace (`@dineeasy/shared-types`) holding the types `apps/customer_web` used to hand-duplicate locally (`Order`/`MenuItem`/`ResolveQrResponse` and everything they nest) — that app's own `src/lib/api/types.ts` is now a one-line re-export of the shared package, so none of the seven files that import from it needed touching. **This is fully built and verified in this sandbox** (unlike almost everything else Prisma-adjacent): `npm install` at the repo root resolves and symlinks the new workspace correctly, `packages/shared_types`'s own `tsc --noEmit` is clean, and `apps/customer_web`'s full `tsc -b && vite build` (and `oxlint`) both still pass against the new import — a genuine end-to-end check, since none of this touches Prisma or the database. What's *not* verified: actually producing the OpenAPI spec, since `SwaggerModule.createDocument()` needs a live app instance, which needs a working `@prisma/client` — see `docs/api.md`'s OpenAPI section for the detail. `services/api`'s own `tsc --noEmit`/`eslint` stayed clean on every file this touched, and the pre-existing 53-error Prisma-typing gap is unchanged.

- **Notifications module** (`modules/notifications/`): a persisted, queryable notification inbox — deliberately a different concern from `RealtimeGateway`'s ephemeral `{type, ids}` WebSocket hints (never stored). `GET /notifications` (optionally `?unread=true`), `GET /notifications/unread-count`, `PATCH /notifications/:id/read`, `PATCH /notifications/read-all` — all authenticated-only (no `@RequirePermission`, matching `PermissionsGuard`'s "undeclared means any signed-in staff member" default), since a staff member's own inbox isn't gated by role. No `POST /notifications`: every row is written by a domain service reacting to a real state change, the same shape as `AuditLogService` (not `@Global()`; a domain module imports `NotificationsModule` explicitly to inject `NotificationsService` — `grep -l NotificationsModule` tells you exactly which modules can create notifications). A notification is either addressed to one staff member (`recipientUserId`) or an outlet-wide broadcast gated by a `requiredPermission` string (null = every staff member at the outlet); `NotificationsService`'s visibility filter — extracted into a pure, Prisma-free function (`notification-visibility.util.ts`) purely so it's genuinely unit-testable in this sandbox — is what enforces this on every read. v1 has exactly one trigger, wired into `OrdersService.transitionStatus` (the single choke point every order status change already passes through, kitchen-driven or staff-driven): an order becoming READY notifies anyone with `orders.view` at the outlet. More triggers are a straightforward addition later; only what's actually wired is built, not speculative coverage of whatever the original spec's notifications section might have said in full (that section's exact text isn't available to this session — see the honesty note in `notifications.service.ts`'s class doc comment). One deliberate v1 simplification: `readAt` lives on the notification row itself, not a per-(notification, recipient) join table, so marking a *broadcast* notification read is shared across every staff member who can see it, not just the one who dismissed it — judged the more useful behavior for the one broadcast type that exists today ("this needs doing" — once anyone's acted on it, it should stop pinging everyone else), documented as a real tradeoff rather than an accidental limitation.

  New Prisma model (`Notification` + `NotificationType` enum) and its own hand-authored migration (`20260908120642_add_notifications`, same reasoning as the initial migration for why it's hand-written) — **and this one actually got the same live-database treatment as the initial migration**: applied to a real, freshly-created Postgres 16 database in this sandbox and diffed against `\d notifications`, confirming every column, index, and foreign-key constraint (including `ON DELETE CASCADE` on all three FKs) landed exactly as the schema specifies. New pure-logic test: `notification-visibility.util.spec.ts` (5 tests, genuinely run — unlike `reports.service.spec.ts`/`auth.service.spec.ts`, this one imports zero Prisma-dependent code, so it isn't blocked by the engine restriction the same way most new backend logic is).

- **Test coverage pass — two more pure-logic extractions**: `BillingService.generateInvoice` used to build its CGST/SGST/IGST breakdown inline, inside the `$transaction` callback — real decimal-rounding logic (round each tax component to 2dp *before* summing, so it matches the printed per-line receipt amounts rather than rounding once at the end) that had no test of its own. Pulled the aggregation into `billing/invoice-tax.util.ts` (`computeInvoiceTaxBreakdown`) the same way `order-pricing.util.ts` and `notification-visibility.util.ts` were — the Prisma fetch of each line's `MenuItem.taxGroup.components` stays in the service, only the pure math moved. 8 new tests (`invoice-tax.util.spec.ts`) actually caught a wrong assumption in a first draft of the test itself — a taxable-amount total I'd hand-computed as unrounded turned out wrong once run, because the function rounds *both* `taxableAmount` and `taxAmount` to 2dp before returning; fixed the test, not the function, once running it showed which one was right. Also covers the documented same-`taxType`-different-`ratePercent` edge case (the emitted rate is whichever line was seen first; amounts still sum correctly across all of them). Separately, `common/utils/require-active-outlet.util.ts` — used by every controller that needs "the current outlet" (menu, tables, orders, notifications, ...) but had never had its own test — got one (`require-active-outlet.util.spec.ts`, 4 tests): the happy path, the missing-outlet 400, asserting the thrown error is actually a `ValidationDomainError` mapped to `httpStatus: 400`/`code: 'ValidationError'` (not just "throws something"), and the empty-string-is-also-missing falsy-check case. Total pure-logic unit tests now **56** (was 44); `npx tsc --noEmit`'s error count is unchanged at **54** — the billing refactor turned one `for...of` loop (not flagged by `noImplicitAny`) into a `.map()` callback (which is), so it needed one explicit inline parameter type to avoid silently adding a 55th error for a change that didn't touch anything Prisma-generated.

- **Flutter staff app — Notifications inbox** (`lib/features/notifications/`): the bell icon + unread badge follow-up to the backend Notifications module above — `NotificationsScreen` (All/Unread toggle, tap-to-mark-read, "mark all read") reading `GET /notifications`/`PATCH /notifications/:id/read`/`PATCH /notifications/read-all`. The bell lives in `home_shell.dart`'s `AppBar` rather than as a nav-shell tab (the wide `NavigationRail` layout gained an `AppBar` it didn't have before, just to host it) — unlike every other feature, it's visible to every signed-in user with no RBAC gating, matching `NotificationsController`'s own authenticated-only contract. `HomeShell` polls `unreadNotificationCountProvider` on a 20-second `Timer.periodic` for as long as the shell is mounted (not just while the inbox screen is open, unlike `KdsScreen`'s narrower 6s poll), now layered with a `RealtimeGateway` `notification.created` subscription for a near-instant badge update — same shape as Kitchen's, see `docs/flutter-app.md`. New pure-logic tests: `test/features/notifications/notification_models_test.dart` (4 tests). See `docs/flutter-app.md` for full detail.

- **Role-assignment removal** — closes a gap flagged when Staff management first landed: the data model always supported multiple simultaneous role assignments per staff member, but there was no way to *remove* one (only add, or replace-for-that-same-outlet). New backend endpoint `DELETE /staff/:id/roles` (`?outletId=` omitted targets the org-wide assignment, the same convention `PATCH /staff/:id`'s `roleName`/`outletId` pairing already uses) — `UsersService.removeRoleAssignment` deletes the one matching `UserRole` row, but refuses (`ValidationDomainError`, 400) to remove a staff member's *only* remaining assignment, since that would zero out their token's permission set and lock them out with no in-app recovery path; the error message points at reassigning a replacement role first, or setting `status: INACTIVE` instead if the real intent is revoking access entirely. On the Flutter side, `_StaffTile`'s role chips (the read/list view, not the edit sheet — see that sheet's own doc comment on why removal isn't duplicated there, to avoid desyncing a snapshot list from the live one) now render a delete icon via `Chip.onDeleted`, gated on `staff.manage` and further hidden per-chip whenever it's that member's only role (mirroring the backend's own refusal locally rather than always round-tripping to find out), behind a confirmation `AlertDialog` — the first use of `showDialog` anywhere in this app, since every earlier destructive-ish action was a full form submission rather than a single tap. `tsc`/`eslint` stayed at the documented baseline (54) for this backend change; no new backend unit test was added for the trivial length-check guard itself, consistent with `create`/`update` in the same service also having no dedicated tests — it's ordinary CRUD-shaped logic, not the kind of standalone computation this project pulls into a pure, tested util.

- **Test coverage pass — payments, and two real bugs it caught**: same pure-logic-extraction pass as billing's tax aggregation, applied to `PaymentsService`'s split-payment and refund math (`recordPayment`'s remaining-balance/overpayment check, the webhook settlement path's "is the order now fully paid" check, and `approveRefund`'s REFUNDED-vs-PARTIALLY_REFUNDED decision) — pulled into `payments/payment-math.util.ts` (`sumSucceededPayments`, `evaluatePaymentAttempt`, `determinePaymentStatusAfterRefund`), 10 new tests. Extracting the webhook and refund-approval logic surfaced **two real, pre-existing double-counting bugs**, not just untestable code: both `handleProviderWebhook` and `approveRefund` re-fetch an aggregate (the order's payments; the payment's processed refunds) *after* already committing the update for the row in question, so the aggregate already includes it — but each then added that same row's amount a second time on top (`.plus(payment.amount...)` in the webhook path; seeding the refund `.reduce()` with `refund.amount` instead of `0` in the refund path), inflating the total and risking marking an order/payment settled before it genuinely was. Both are currently dormant in production terms (v1 has no real payment gateway wired up to call the webhook — see `PaymentsService`'s class doc comment — and a refund needs at least two approvals for the bug to produce a *wrong* status rather than just an early-but-still-correct one), but they were real bugs in the logic itself, the kind hand-review alone hadn't caught across however many times this file had been read before. Fixed by having both call sites sum the post-update aggregate on its own, with no separate addition. `recordPayment`'s own validation path was already correct (it evaluates *before* creating the payment, so no double-count was possible there) and needed no logic change beyond routing through the same extracted function. Pure-logic unit test total: 56 → 66. `npx tsc --noEmit`'s error count actually *dropped*, 54 → **52** — the refund-total `.reduce()` callback this touched already had two pre-existing implicit-`any` parameters before this pass (same class of gap as billing's), and adding explicit types while fixing the double-count removed them from the count as a side effect, not a regression.

- **Test coverage pass — kitchen status derivation**: `KitchenService.recomputeOrderStatus` is the literal implementation of spec §8's "the order status reflects what's actually happening in the kitchen, not a separate staff-declared flag" — deciding whether a kitchen-driven order (PLACED/ACCEPTED/PREPARING/READY) should advance to PREPARING or READY based on every `KitchenOrderItem` status underneath it — but had never had a test of its own despite being one of the more consequential branching-logic spots in the backend. Extracted the decision itself (excluding cancelled items from consideration; "all done" only when every relevant item is READY/COMPLETED; "started" when any relevant item has left NEW) into `kitchen/kitchen-status.util.ts` (`deriveKitchenDrivenOrderStatus`), 9 new tests — including the vacuous-truth edge case a naive `.every()` over an all-cancelled ticket would get wrong (an empty-after-filtering array trivially satisfies `.every(...)`, which would otherwise read as "all done" for a ticket where nothing was actually completed, just called off entirely; the function special-cases `relevant.length === 0` to return "no change" instead). `advanceOrderTo` (the forward-only clamp against the order's *current* status) stays in the service — it's the part that genuinely needs a live order read, not something worth faking a Prisma shape for. Pure-logic unit test total: 66 → 75. `npx tsc --noEmit`'s error count dropped again, 52 → **49** — the three `.filter`/`.every`/`.some` callbacks this replaced each had their own pre-existing implicit-`any` parameter (same Prisma-stub class of gap), all removed by routing through the now-typed pure function instead.

- **Customer PWA — installability** (`apps/customer_web`): `vite-plugin-pwa` wired into `vite.config.ts`, generating a real `manifest.webmanifest` + service worker at build time — the app is now add-to-home-screen installable with a standalone window and its own icon (`public/icon-192.png`/`icon-512.png`/`icon-maskable-512.png`, rendered from the existing `favicon.svg` mark via `rsvg-convert`, the maskable variant padded onto the app's actual `#e85d2c` accent color so it survives a circular OS icon mask). Deliberately scoped to installability and a fast-loading precached app shell only — zero `runtimeCaching` rules, so `/api/*` traffic is never intercepted by the service worker, matching `docs/offline-mode.md`'s explicit "no offline queue on the guest side" design rather than quietly working around it. Verified concretely, not just configured: `npm run build` actually produces the manifest and `dist/sw.js`, and both were read directly to confirm the icons/colors/`start_url` match and that the service worker's only registered route is the SPA navigation fallback with `/api/` denylisted — see `docs/customer-web.md`'s Verification status section for the full detail, including a known low-risk `npm audit` advisory in `vite-plugin-pwa`'s own build-time dependency chain.

- **Order domain — settled-status constant, after auditing a look-alike duplication**: a grep for repeated "which order statuses count as done" literals turned up five call sites across `orders.service.ts` (3), `dining-sessions.service.ts` (1), and `payments.service.ts` (1) that looked like accidental drift at a glance (`['BILLED', 'PAID', 'COMPLETED', 'CANCELLED', 'REFUNDED']` vs `['PAID', 'COMPLETED', 'CANCELLED', 'REFUNDED']` vs `['COMPLETED', 'CANCELLED', 'REFUNDED']` vs an allow-list `['BILLED', 'PAID']`). Reading each call site in context showed only two of the five actually encode the same rule: `OrdersService.applyDiscount` and `DiningSessionsService.close` both guard on "has this order's money side already concluded" with the identical 4-status list, so that pair was a genuine duplicate, extracted into `ORDER_FINANCIALLY_SETTLED_STATUSES` / `isOrderFinanciallySettled()` in `common/order/order-state-machine.ts` (the existing single-source-of-truth file for order status), with 3 new tests. The other three were deliberately left alone and each got an explanatory comment instead of being folded in, because they're genuinely different rules that only coincidentally share status names: `cancelItem` locks one status earlier, at BILLED, because a generated bill's line items must stay frozen even before it's paid; `listActiveForOutlet`'s "still on the board" set treats BILLED and PAID as still active (staff still need to see them) and excludes only the fully-done end states; and `recordPayment`'s check is an allow-list of exactly which two statuses a payment can be taken against, not a settled/unsettled check at all. Forcing all five into one constant would have been a false-consistency bug in the making — the whole point of the audit was to fix the one real duplicate without inventing a wrong one. Pure-logic unit test total: 75 → 78. `npx tsc --noEmit`'s error count is unchanged at **49** — this pass touched call sites that were already implicit-`any` on the same pre-existing Prisma-stub class of gap and left them exactly as typed as before.

- **Print agent** (`services/print-agent`, new npm workspace, `@dineeasy/print-agent`): the piece `docs/printing.md` had explicitly flagged as "not implemented — a separate, out-of-repo concern" is now built in-repo as a standalone Node/TypeScript daemon — ESC/POS ticket rendering (`escpos.ts`: pure functions, KOT and receipt layouts, ASCII-only with "Rs." money formatting since the default printer codepage has no ₹ glyph), raw TCP delivery to port 9100 (`printer-socket.ts`), an API client with reactive 401→refresh→retry (`api-client.ts`), and the poll-loop orchestration tying them together (`poll-loop.ts`), all wired up by a thin `index.ts` entrypoint. Verified at three levels of fidelity rather than either all-mocked or not-verified-at-all: `escpos.spec.ts` is pure-function testing; `printer-socket.spec.ts` and `api-client.spec.ts` each stand up a *real* local server (`net`/`http`) in place of the printer/API respectively and drive real TCP/HTTP traffic against it, including proving the token-refresh path actually executes (the fake API only accepts the new token once `/auth/refresh` was actually called correctly); `poll-loop.spec.ts` tests the orchestration decisions against in-memory fakes of those two already-proven pieces. Beyond the 45 unit tests, the compiled agent (`npm run build && node dist/index.js`) was run for real against a throwaway fake API + fake TCP printer server as a one-off smoke test — a genuine end-to-end pass (login → poll → render → send → status report) with the actual bytes the fake printer received inspected directly, not assumed. `npx tsc --noEmit` and `eslint` are both clean with zero errors on this package. What's not verified, because it can't be in this sandbox: a physical ESC/POS printer, and a real (non-fake) `services/api` on the other end (still blocked on `prisma generate`, `docs/troubleshooting.md`). See `services/print-agent/README.md` for the full detail.

- **Test coverage pass — the guest token trust boundary**: `common/guest-auth/guest-token.service.ts` (sign/verify for the no-signup QR ordering flow's bearer token — spec §4/§12) turned out to be the one piece of `services/api`'s auth surface with *no* Prisma dependency at all (just `jsonwebtoken` + a config lookup), and yet had never had a test despite being the entire trust boundary for that flow: every `/qr/*` request's tenant scoping comes from what this file decodes, nothing downstream re-derives it (see `QrService.resolveTokenAndJoinSession`'s doc comment on why that's the one deliberate pre-tenant lookup in the system). 8 new tests (`guest-token.service.spec.ts`) cover the sign→verify round trip, the embedded 12h TTL, and every rejection path failing closed as the intended `UnauthenticatedDomainError` (401) rather than leaking a raw JWT error or — worse — silently accepting something it shouldn't: garbage input, a token signed with the wrong secret, an expired token, and a token whose payload was tampered with after signing (re-encoding a different `outletId` into the claims segment while keeping the original signature — the exact shape of attack that would otherwise leak one outlet's guest ordering session into another's). Pure-logic unit test total: 78 → 86; `npx tsc --noEmit`'s error count unchanged at **49** (this file had zero Prisma-stub implicit-`any`s to begin with, being Prisma-free).

- **First real run against a live network — two real bugs it caught**: everything above was verified as far as this sandbox's Prisma-engine restriction allows; the first time `prisma migrate dev`/`seed` actually ran end-to-end on a machine with normal internet access, it surfaced two real, previously-invisible bugs immediately — a `DATABASE_URL`-not-found failure caused by `@nestjs/config`/Prisma CLI resolving `.env` relative to `services/api`'s cwd rather than the monorepo root (fixed: `ConfigModule`'s `envFilePath` fallback plus `dotenv-cli` wrapping the `prisma:*`/`seed` scripts — root `.env` stays the one source of truth), and a genuine seed-script bug where `userRole.upsert` used a compound unique key (`userId_roleId_outletId`) with a `null` `outletId`, which Prisma's generated types never allow for a nullable compound-key member (fixed: `findFirst` + manual `create`, matching the same workaround `UsersService.removeRoleAssignment` already used elsewhere for the identical nullable-`outletId` shape). Both fixes were verified against a *real* generated `@prisma/client`, not just read for plausibility — the first time anything in this repo touching the generated client had that level of verification. Full detail in `docs/troubleshooting.md`.

- **First full local run end-to-end, and everything it caught** — Docker, a real Postgres/Redis, a real generated Prisma client, both host apps, and a physical Android device were all run together for the first time anywhere in this project's history (previously only individually smoke-tested, per the bullets above). This surfaced a long tail of real, previously-invisible bugs across the whole stack: 8 real `tsc` errors on the first genuine compile against a real (non-stub) generated client (`Prisma.InputJsonValue`/enum casts, optional-chaining on `_sum`); a subtle `AsyncLocalStorage`-vs-Prisma-laziness bug in `TenantContextStore.runUnscoped` that broke every pre-tenant lookup including login itself (root-caused and fixed with a real regression test proving it, not just plausibility — see `tenant-context.spec.ts`); `resolveActiveOutlet` never falling back to an org's sole outlet for an org-wide role, dead-ending Owner login; `@IsUUID()` DTO validators rejecting `seed.ts`'s deliberate human-readable ids; and the "Mark served" and realtime-wiring gaps described in their own bullets above. Full diagnosis-and-fix detail for each — including the dev-environment-only issues (Docker Desktop port/restart quirks, the `--env-file` `.env`-autoload gotcha, CORS/LAN-IP configuration for a phone reaching `dev:api`/`dev:web`, a regenerated table QR token no longer matching its seeded demo value) — is in `docs/troubleshooting.md`, not repeated here since none of it is architecture, just what a first real run against real infrastructure actually looks like.

- **POS web app** (`apps/pos_web`, new npm workspace — Vite + React + TS, same stack as `apps/customer_web`): a second, independent staff client of the same backend the Flutter app talks to — sign in, work the table/takeaway floor, build and send orders, bill, and take payment, all from a browser rather than the installed Flutter app. Staff auth with proactive+reactive access-token refresh de-duplicated behind one in-flight promise (`lib/api/client.ts`), RBAC-gated UI reading the same `PERMISSIONS` catalog the backend enforces, an in-memory-only cart (no persistence — a POS terminal doesn't need a reload-surviving cart, unlike the guest app's session-scoped one), and a `RealtimeGateway` subscription for live table/order updates across terminals. Deliberately not feature-identical to the Flutter app in v1 — no KDS or back-office management here, see `docs/pos-web.md` for the exact scope split. Building it is also what turned `packages/shared_types` into a real shared package rather than "the customer web app's types" — see §13 above and the centralized-theme bullet below.
- **Centralized color theme** (`packages/shared_types/src/theme.ts`): both web apps' color palette now lives in one place — a `theme` token object plus `applyTheme()`, which writes it onto `document.documentElement` as the same CSS custom properties (`--accent`, `--veg`, ...) each app's stylesheet already referenced. Before `apps/pos_web` existed, `apps/customer_web/src/index.css`'s `:root` block was the only copy of these values; a second web app meant either copy-pasting it (silent-drift risk) or centralizing once, which is what happened — each app's `main.tsx` now calls `applyTheme()` before its first render, and each `index.css` keeps a literal fallback copy of the identical values (belt-and-braces only; `applyTheme()` overwrites them in normal operation). The Flutter app's seed color (`_brandSeed` in `app.dart`) is the same accent, kept in sync by hand for the same Dart-can't-import-TypeScript reason as the RBAC catalog; unlike the two web apps (hand-picked literal token values), it derives its `ColorScheme` from that seed via `ColorScheme.fromSeed(..., dynamicSchemeVariant: DynamicSchemeVariant.vibrant)`, a deliberately punchier/more saturated M3 palette than the default `tonalSpot` variant — see `docs/pos-web.md`'s theme section and `_buildTheme`'s doc comment in `app.dart`. Verified concretely: both apps' `tsc -b && vite build` pass clean against the change, and `packages/shared_types`'s own `tsc --noEmit` is clean (required adding `DOM` to that package's `lib` array, since `applyTheme` is the package's first browser-only code — see `theme.ts`'s doc comment). The Flutter theme change itself can't be run/analyzed in this sandbox (no Dart/Flutter SDK — see `docs/troubleshooting.md`); it was reviewed by hand and needs a real `flutter run`/hot-reload to confirm visually.
- **Print bill + Printers screens (both apps) + real USB delivery**: `POST /invoices/:id/print` (`BillingService.printInvoice`) is an explicit, repeatable reprint alongside the automatic single print `generateInvoice` already did — surfaced as a "Print bill" button in both billing detail screens. Prompted by a real setup gap: a restaurant with an Epson TM-T82X had nowhere in either app to actually register a `Printer` row — `services/print-agent/README.md` always assumed a "Settings → Printers" screen existed, but neither app had built one, so the only path was a raw `POST /printers` call. `apps/pos_web/src/features/printers/PrintersScreen.tsx` closed that gap first; `apps/restaurant_app/lib/features/printers/` (reached from a new `SettingsScreen`, see the next bullet) is the same screen ported into Flutter. Both are `printers.manage`-gated, list + add a printer, `NETWORK` (IP/port) or `USB`. `USB` used to be accepted but silently undrained — `services/print-agent/src/printer-usb.ts` now actually delivers to it (the `usb`/libusb npm package, standard USB Printer-class discovery, vendor-independent) — see `docs/printing.md`.

**In progress / not yet built:**
- ~~Flutter's Settings nav destination is still a placeholder~~ — done: `SettingsScreen` (account info, sign-out, and a `printers.manage`-gated Printers entry) replaces the old placeholder; its Printers screen is the direct Flutter port mentioned in the Print bill bullet above.
- Every permission-gated feature-area destination in the Flutter nav shell now has a real screen — POS, Tables management, Kitchen/KDS, Billing, Reports, Staff, and Settings. No placeholder destinations remain.
- Online payment in the customer PWA (no gateway is wired up anywhere in v1 — see `docs/payments.md`) and guest order history/accounts (deliberately out of scope, spec §4/§64). The installable-PWA service worker/manifest that used to be listed here is done — see the customer-web bullet below and `docs/customer-web.md`.
- The Flutter app still hand-mirrors backend types per screen (`lib/features/*/data/*_models.dart`) — an unavoidable, permanent gap across the Dart/TypeScript language boundary, not something `packages/shared_types` (TypeScript-only, see below) closes. See `docs/flutter-app.md`.
- Integration/e2e tests against a live, fully-generated `services/api`. The print agent that used to be listed here (see `docs/printing.md`) is now built — `services/print-agent`, see the bullet above.
- OS-level push/local notifications for the Notifications module — still not built. The more basic step, a Flutter-side in-app inbox (bell icon + unread badge reading `GET /notifications`), is now done (`lib/features/notifications/`, see `docs/flutter-app.md`); this line is now specifically about real system-level push delivery, not the in-app inbox.
- `apps/restaurant_app`'s `android`/`windows` platform folders, `flutter pub get`/`analyze`/`test`, and `prisma generate`/`@prisma/client` were all blocked in *this* sandbox specifically (no Flutter/Dart SDK, no network path to `binaries.prisma.sh` — see `docs/troubleshooting.md`) — but all of it **has now actually been run for real**, on the developer's own machine with normal internet access: `flutter create .` generated the platform folders, the app built and ran on a physical Android device, `prisma generate`/`migrate dev`/`seed` all ran against a real Postgres, and a full order lifecycle (place → kitchen → serve → bill → pay) was exercised end to end through the real running API and a real running Flutter app. See the "First full local run end-to-end" bullet above and `docs/troubleshooting.md` for everything that surfaced and got fixed along the way. What's *still* genuinely open: `flutter analyze`/`flutter test` themselves (the app running and being manually exercised isn't the same as the test suite and linter actually passing — neither has been confirmed clean yet), and this sandbox specifically still can't do any of the above itself, so every change made here continues to need a real run on a real machine before it's trusted, same as always.
- Integration/e2e tests against the now-real, fully-generated `services/api` — the live manual run above exercised the real thing, but there's still no automated e2e suite running against it (`test:e2e`, `docker:test` — see `docs/local-development.md`) actually confirmed green.

Everything above the "Done" line in this document is the target architecture these next slices are built against, not a description of already-shipped code.
