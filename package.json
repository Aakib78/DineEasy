{
  "name": "dineeasy",
  "private": true,
  "version": "0.1.0",
  "description": "DineEasy — Run your restaurant, effortlessly. Monorepo root.",
  "workspaces": [
    "services/api",
    "services/print-agent",
    "apps/customer_web",
    "apps/pos_web",
    "packages/shared_types"
  ],
  "scripts": {
    "dev:api": "npm run start:dev --workspace services/api",
    "dev:web": "npm run dev --workspace apps/customer_web",
    "dev:pos": "npm run dev --workspace apps/pos_web",
    "dev:print": "npm run dev --workspace services/print-agent",
    "dev:all": "npm run docker:up && concurrently -n api,pos,web -c blue,green,yellow \"npm:dev:api\" \"npm:dev:pos\" \"npm:dev:web\"",
    "stop:all": "bash scripts/stop-all.sh",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "docker:up": "docker compose --env-file .env -f infrastructure/docker/docker-compose.yml -f infrastructure/docker/docker-compose.dev.yml up -d",
    "docker:down": "docker compose --env-file .env -f infrastructure/docker/docker-compose.yml -f infrastructure/docker/docker-compose.dev.yml down",
    "docker:prod": "docker compose --env-file .env -f infrastructure/docker/docker-compose.yml up -d",
    "docker:test": "POSTGRES_PASSWORD=dineeasy_test_only docker compose -f infrastructure/docker/docker-compose.yml -f infrastructure/docker/docker-compose.test.yml up -d"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
